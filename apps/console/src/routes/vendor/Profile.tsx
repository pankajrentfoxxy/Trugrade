import * as React from 'react';
import { ProfileHub } from './profile/ProfileHub';

/** Supplier profile — six progressive sections in dialogs. */
export function VendorProfileRoute(): React.JSX.Element {
  return <ProfileHub />;
}
